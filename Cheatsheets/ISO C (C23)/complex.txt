//|----------------------------------------------------------------------------
//| Trigonometric Functions (complex.h) — C99+
//|----------------------------------------------------------------------------

double complex        cacos        (double complex z);                           /* Computes the complex arc cosine of z. */
float complex         cacosf       (float complex z);                            /* Computes the complex arc cosine of z (float). */
long double complex   cacosl       (long double complex z);                      /* Computes the complex arc cosine of z (long double). */

double complex        casin        (double complex z);                           /* Computes the complex arc sine of z. */
float complex         casinf       (float complex z);                            /* Computes the complex arc sine of z (float). */
long double complex   casinl       (long double complex z);                      /* Computes the complex arc sine of z (long double). */

double complex        catan        (double complex z);                           /* Computes the complex arc tangent of z. */
float complex         catanf       (float complex z);                            /* Computes the complex arc tangent of z (float). */
long double complex   catanl       (long double complex z);                      /* Computes the complex arc tangent of z (long double). */

double complex        ccos         (double complex z);                           /* Computes the complex cosine of z. */
float complex         ccosf        (float complex z);                            /* Computes the complex cosine of z (float). */
long double complex   ccosl        (long double complex z);                      /* Computes the complex cosine of z (long double). */

double complex        csin         (double complex z);                           /* Computes the complex sine of z. */
float complex         csinf        (float complex z);                            /* Computes the complex sine of z (float). */
long double complex   csinl        (long double complex z);                      /* Computes the complex sine of z (long double). */

double complex        ctan         (double complex z);                           /* Computes the complex tangent of z. */
float complex         ctanf        (float complex z);                            /* Computes the complex tangent of z (float). */
long double complex   ctanl        (long double complex z);                      /* Computes the complex tangent of z (long double). */


//|----------------------------------------------------------------------------
//| Hyperbolic Functions (complex.h) — C99+
//|----------------------------------------------------------------------------

double complex        cacosh       (double complex z);                           /* Computes the complex arc hyperbolic cosine of z. */
float complex         cacoshf      (float complex z);                            /* Computes the complex arc hyperbolic cosine of z (float). */
long double complex   cacoshl      (long double complex z);                      /* Computes the complex arc hyperbolic cosine of z (long double). */

double complex        casinh       (double complex z);                           /* Computes the complex arc hyperbolic sine of z. */
float complex         casinhf      (float complex z);                            /* Computes the complex arc hyperbolic sine of z (float). */
long double complex   casinhl      (long double complex z);                      /* Computes the complex arc hyperbolic sine of z (long double). */

double complex        catanh       (double complex z);                           /* Computes the complex arc hyperbolic tangent of z. */
float complex         catanhf      (float complex z);                            /* Computes the complex arc hyperbolic tangent of z (float). */
long double complex   catanhl      (long double complex z);                      /* Computes the complex arc hyperbolic tangent of z (long double). */

double complex        ccosh        (double complex z);                           /* Computes the complex hyperbolic cosine of z. */
float complex         ccoshf       (float complex z);                            /* Computes the complex hyperbolic cosine of z (float). */
long double complex   ccoshl       (long double complex z);                      /* Computes the complex hyperbolic cosine of z (long double). */

double complex        csinh        (double complex z);                           /* Computes the complex hyperbolic sine of z. */
float complex         csinhf       (float complex z);                            /* Computes the complex hyperbolic sine of z (float). */
long double complex   csinhl       (long double complex z);                      /* Computes the complex hyperbolic sine of z (long double). */

double complex        ctanh        (double complex z);                           /* Computes the complex hyperbolic tangent of z. */
float complex         ctanhf       (float complex z);                            /* Computes the complex hyperbolic tangent of z (float). */
long double complex   ctanhl       (long double complex z);                      /* Computes the complex hyperbolic tangent of z (long double). */


//|----------------------------------------------------------------------------
//| Exponential, Logarithmic, and Power Functions (complex.h) — C99+
//|----------------------------------------------------------------------------

double complex        cexp         (double complex z);                           /* Computes the complex base-e exponential of z. */
float complex         cexpf        (float complex z);                            /* Computes the complex base-e exponential of z (float). */
long double complex   cexpl        (long double complex z);                      /* Computes the complex base-e exponential of z (long double). */

double complex        clog         (double complex z);                           /* Computes the complex natural logarithm of z. */
float complex         clogf        (float complex z);                            /* Computes the complex natural logarithm of z (float). */
long double complex   clogl        (long double complex z);                      /* Computes the complex natural logarithm of z (long double). */

double complex        cpow         (double complex x, double complex y);         /* Computes x raised to the power y. */
float complex         cpowf        (float complex x, float complex y);           /* Computes x raised to the power y (float). */
long double complex   cpowl        (long double complex x, long double complex y); /* Computes x raised to the power y (long double). */

double complex        csqrt        (double complex z);                           /* Computes the complex square root of z. */
float complex         csqrtf       (float complex z);                            /* Computes the complex square root of z (float). */
long double complex   csqrtl       (long double complex z);                      /* Computes the complex square root of z (long double). */


//|----------------------------------------------------------------------------
//| Manipulation Functions (complex.h) — C99+
//|----------------------------------------------------------------------------

double                cabs         (double complex z);                           /* Computes the magnitude (absolute value) of z. */
float                 cabsf        (float complex z);                            /* Computes the magnitude of z (float). */
long double           cabsl        (long double complex z);                      /* Computes the magnitude of z (long double). */

double                carg         (double complex z);                           /* Computes the phase angle (argument) of z. */
float                 cargf        (float complex z);                            /* Computes the phase angle of z (float). */
long double           cargl        (long double complex z);                      /* Computes the phase angle of z (long double). */

double                cimag        (double complex z);                           /* Returns the imaginary part of z. */
float                 cimagf       (float complex z);                            /* Returns the imaginary part of z (float). */
long double           cimagl       (long double complex z);                      /* Returns the imaginary part of z (long double). */

double complex        conj         (double complex z);                           /* Computes the complex conjugate of z. */
float complex         conjf        (float complex z);                            /* Computes the complex conjugate of z (float). */
long double complex   conjl        (long double complex z);                      /* Computes the complex conjugate of z (long double). */

double complex        cproj        (double complex z);                           /* Computes the projection of z onto the Riemann sphere. */
float complex         cprojf       (float complex z);                            /* Computes the projection of z (float). */
long double complex   cprojl       (long double complex z);                      /* Computes the projection of z (long double). */

double                creal        (double complex z);                           /* Returns the real part of z. */
float                 crealf       (float complex z);                            /* Returns the real part of z (float). */
long double           creall       (long double complex z);                      /* Returns the real part of z (long double). */

double complex        CMPLX        (double x, double y);                         /* C11+. Constructs a double complex value with real part x and imaginary part y, without arithmetic side effects (e.g. NaN/inf propagation issues). Macro. */
float complex         CMPLXF       (float x, float y);                           /* C11+. Constructs a float complex value from x and y. Macro. */
long double complex   CMPLXL       (long double x, long double y);               /* C11+. Constructs a long double complex value from x and y. Macro. */


//|----------------------------------------------------------------------------
//| Macros (complex.h) — C99+
//|----------------------------------------------------------------------------

complex                                                                          /* Expands to the keyword _Complex. */
_Complex_I                                                                       /* A constant of type const float _Complex representing the imaginary unit; always defined. */
I                                                                                 /* Expands to _Complex_I, or to _Imaginary_I if the implementation supports the imaginary type; represents the imaginary unit. */
imaginary                                                                        /* OPTIONAL. Expands to the keyword _Imaginary. Only defined if the implementation supports the imaginary type (rare — not supported by glibc, clang, or MSVC). */
_Imaginary_I                                                                     /* OPTIONAL. A constant of type const float _Imaginary representing the imaginary unit. Only defined if the imaginary type is supported. */
