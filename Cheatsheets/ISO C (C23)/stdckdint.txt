//|----------------------------------------------------------------------------
//| Version Macro (stdckdint.h)
//|----------------------------------------------------------------------------

__STDC_VERSION_STDCKDINT_H__                                                    /* Integer constant denoting the version of stdckdint.h supported. */


//|----------------------------------------------------------------------------
//| Checked Integer Arithmetic (stdckdint.h)
//|----------------------------------------------------------------------------

bool          ckd_add       (type1 *result, type2 a, type3 b);                  /* Computes a + b, stores the result in *result, and returns true if overflow occurred. */
bool          ckd_sub       (type1 *result, type2 a, type3 b);                  /* Computes a - b, stores the result in *result, and returns true if overflow occurred. */
bool          ckd_mul       (type1 *result, type2 a, type3 b);                  /* Computes a * b, stores the result in *result, and returns true if overflow occurred. */